import { createSelector } from '@reduxjs/toolkit';

// export const user = (state) => state.authentication.user;
// export const userSelector = createSelector(user, (userSelect) => userSelect);
// export const userSelector = createSelector(user);
